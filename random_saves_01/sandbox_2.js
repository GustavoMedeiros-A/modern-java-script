// common string methods

let email = 'mario@thenetninja.com.uk';

//let result = email.lastIndexOf('n'); // Find the last occurrence of the specified substring

//let result = email.slice(0, 5); // extracts a section of a string and returns it as a new string, without modifying the original string.

// let result = email.substr(4, 10);

//let result = email.replace('m', 'w'); // Replace a certain character to another character (M to W in this case)
// Catch the first element 



console.log(result);
